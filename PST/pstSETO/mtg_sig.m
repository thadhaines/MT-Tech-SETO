function mtg_sig(k)
% Syntax: f = mtg_sig(t,k)
% 12:37 PM 7/0/98
% defines modulation signal for turbine power reference
%global tg_sig n_tg n_tgh
% global g
% 
% if g.tg.n_tg~=0 || g.tg.n_tgh~=0
%   g.tg.tg_sig(:,k) = zeros(g.tg.n_tg+g.tg.n_tgh,1);
%   if g.sys.t(k)<=0.0
%      g.tg.tg_sig(:,k) = zeros(g.tg.n_tg+g.tg.n_tgh,1);
%   else
%      g.tg.tg_sig(:,k) = zeros(g.tg.n_tg+g.tg.n_tgh,1);
%      %g.tg.tg_sig(1,k) = -1.0*t;
%      %g.tg.tg_sig(1,k) = -0.01;
%   end
% end

return
